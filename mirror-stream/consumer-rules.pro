# Consumer ProGuard rules for the mirror-stream library.
# These rules are applied to any app that consumes this library.
# Currently empty — public API is plain Kotlin and is not stripped under R8 by default.
